# self checking armv6-m instruction test. standalone assembly with its own
# vector table, so it does not link the bsp startup code
APP  := isatest
SRCS := isatest.S

BSP_SRCS :=

include ../../bsp/bsp.mk
