# randomised armv6-m instruction test. the .S is generated by
# tools/isagen.py from its own reference model, so it is not checked in as
# something to hand edit
APP  := isarand
SRCS := isarand.S

BSP_SRCS :=

include ../../bsp/bsp.mk
